<h1>Edit a Pin!</h1>

<% if !@errors.nil? %>
	<div class="errors">
		<label><%= pluralize(@errors.count, "error") %> prohibited this pin 
		from being saved:</label>
			<ul>
				<% @errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
	</div>
<% end %>

<%= render "form" %>

<div class="back">
<a href="/">
<i class="fa fa-arrow-left"></i>  Back to Home
</a>
</div>
